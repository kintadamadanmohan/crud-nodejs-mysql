version: '3.1'

services:

  version: '3.1'

services:

  mysql:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password

  demo-app:
    image: demo-app:1.0
    restart: always
    ports:
      - 3001:3000
    